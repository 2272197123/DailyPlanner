# 2026-08-03

## DailyPlan v6.0 大版本升级（场景：网站开发）

用户提出 5 个问题，全部解决并写入 README：

1. **存档修复**：archive.js 重写。修复存档对象用 `store.currentDate`（浏览日期）而非真实今天的 bug；新增凌晨补档（昨天漏存自动补）；存档内容补全（反馈/评分/时间轴配置/summary）；双写 LS + 服务器 archives 表。新增存档查看面板（顶栏「📦 存档」）。
2. **动画升级**：FLIP 拖拽重排、入场级联（`store._tlFresh` 一次性标志，避免动画重播）、时间点重算脉冲、进度环百分比 rAF 数字滚动、prefers-reduced-motion 降级。
3. **统一时间轴**：新建 js/timeline.js + css/timeline.css。合并「每日固定」与「学习任务」为单时间轴；每项只显示用时，开始/完成时间 = 起点 + 前序累加动态计算；整行拖拽（上下半区插入指示线），drop 后实时重算并持久化到 `dp_timelineCfg[date]={start,order}` + `PUT /api/order/{date}`。旧 renderRoutines/renderTasks/旧拖拽已删除。
4. **前后端整合**：launcher.py v8.0 优先 uvicorn 启动 FastAPI（静态+API 同源 5000 端口），未装 fastapi 时回退静态服务器。**修复 db.py SQLite 方言转换 2 个 v4.2 遗留 bug**：反引号标识符 excluded 正则残留右括号（set_state 必崩）、INSERT IGNORE 未转 INSERT OR IGNORE（mark_earned 必崩）——后端此前从未真正跑通。新增 archives 表与 5 个端点（archive×3、order×2）、no-cache 中间件。
5. **README 实时更新**：v6.0 变更记录、结构、启动方式、API 表、localStorage 键表（新增 dp_timelineCfg）、Agent 提示重编号至 38 条。

## 关键约定（后续迭代遵守）
- 时间轴渲染入口 `renderTimeline()`；顺序存 `dp_timelineCfg` + 服务器；导入新计划须 `clearTimelineCfg(date)`。
- `store._tlFresh` 只在日期切换/导入/首屏设置（一次性入场动画）。
- db.py SQL 约定：INSERT 的 `VALUES (` 带空格，ON DUPLICATE 后的 `VALUES(col)` 不带空格。
- 系统 Python 3.10（`C:\Users\Ahsoka·Tano\AppData\Local\Programs\Python\Python310\python.exe`）已装 fastapi 0.111 + uvicorn 0.30，可跑后端；托管 Python 3.13 未装。
- 用户约定：每次改动须同步更新 study_planner/README.md。

## v6.1 细节优化（同日第二轮）

1. **拖拽修复根因**：卡片文本选择劫持 HTML5 dragstart → `.tl-item` 加 `user-select:none`。这是"学习任务拖不动、日常项能拖"的真正原因（卡片文字多）。
2. **JSON 格式 v6.1**：`duration` 为主必填，`time` 可选（仅排序锚点），新增 `startTime` 字段导入时写入时间轴配置。
3. **routine 行显示晶圆徽章**（+50円/已获得）。
4. **记账重构**（参考极简记工时）：按日中心视图 + 7 日红绿条图 + chips 录入 + `updateAccountingEntry()` 编辑任意条目。收支配色定为**收入红/支出绿**（中文习惯）。
5. **卡片重构**：标题 .98rem/700 + 用时胶囊 card-dur + 奖励徽章放大 + 计时按钮文案化。
6. **陷阱**：overlay-anim.js 覆写的 openAccounting 只调 renderAccounting，不执行 ledger.js 里的初始化 → renderAccounting 顶部加了 `!_accDate` 防御初始化。所有 overlay 打开函数已互相关闭 archiveOverlay。
7. rm 删除 js 目录下临时文件在沙箱中会失败（safe-delete 路径问题），用 PowerShell Remove-Item 替代。
